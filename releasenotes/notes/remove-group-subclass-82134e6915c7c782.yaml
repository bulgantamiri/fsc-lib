---
upgrade:
  - |
    The ``fsc_lib.command.commandmanager.CommandManager`` class is a
    direct subclass of ``cliff.commandmanager.CommandManager``. The extra
    fsc_lib functionality has been shifted upstream into cliff.
